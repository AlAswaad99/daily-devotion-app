NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Deliberately absent: SUPABASE_SERVICE_ROLE_KEY. The dashboard runs under the
# admin's own session so RLS applies — a service-role key would step over the
# policy that keeps reflections private.
