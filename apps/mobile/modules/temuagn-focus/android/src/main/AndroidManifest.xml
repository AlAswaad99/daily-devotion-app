<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Granted once by the member on a system settings screen; there is no dialog. -->
  <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <!--
      `specialUse` because none of Android 14's other foreground service types
      describe a prayer timer. It is declared honestly rather than borrowing a type
      that would pass review by misdescribing what the service does.
    -->
    <service
      android:name="org.temuagn.focus.FocusService"
      android:exported="false"
      android:foregroundServiceType="specialUse"
      android:stopWithTask="false">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="A user-started, time-bounded prayer session that silences notifications and must restore them when it ends." />
    </service>

    <receiver
      android:name="org.temuagn.focus.RestoreReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
