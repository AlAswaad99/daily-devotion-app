/**
 * Where the pre-profile language choice is remembered. Once a profile exists the
 * account setting is authoritative and synced across devices; this only carries the
 * choice from the sign-in screen through to onboarding.
 */
export const LANGUAGE_KEY = 'abide.language'
