import { ComingLater } from '../../src/components/ComingLater'

/** Phase 7, and gated on the Biblica permission — see the licensing gate. */
export default function Bible() {
  return <ComingLater titleKey="bibleTab" bodyKey="bibleComing" />
}
